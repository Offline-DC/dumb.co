{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for our own windows (main + the fullscreen QR window). The remote Google sign-in window is intentionally excluded. App commands (open_login, open_qr_window, get_qr_svg) are allowed by the invoke handler.",
  "windows": ["main", "qr"],
  "permissions": ["core:default"]
}
